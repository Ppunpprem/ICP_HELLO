import React from 'react';
import ReactDOM from 'react-dom/client';
import ExampleForm from './NameInput';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <main>
      <ExampleForm />
    </main>
  </React.StrictMode>
);
